name: Patch RSVP card

on:
  push:
    paths:
      - '.github/workflows/patch-rsvp.yml'

permissions:
  contents: write

jobs:
  patch-rsvp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply RSVP update
        run: python patch_rsvp.py
      - name: Commit changes
        run: |
          if git diff --quiet -- index.html; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Add personalized RSVP card"
          git push origin HEAD:main
